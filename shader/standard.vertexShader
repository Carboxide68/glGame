#version 330 core

layout(location = 0) in vec3 vPos;
layout(location = 1) in vec3 vNormal;

uniform mat4 assembledMatrix;
uniform mat4 model;

out vec3 FragPos;

void main() {

    gl_Position = assembledMatrix * vec4(vPos, 1.0f);
    FragPos = vec3(model * vec4(vPos, 1.0));

}